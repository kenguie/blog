== README

Rails application following Railsguide in order to create a Blogging site. 

Actually the progenitor idea to my Restauranteur site called Tweat On Food.

### Set Up
Pull down the repository:

git clone 

bundle your gems:

Setup the database:

rake db:setup

Start up the server on your local machine:

rails s

Go to localhost:3000 in your browser.
